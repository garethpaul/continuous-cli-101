{
  "name": "continuous-cli101",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "node scripts/test-functions.js",
    "check": "scripts/check-baseline.sh",
    "verify": "npm test && npm run check && npm run audit",
    "start": "twilio-run",
    "deploy": "twilio-run deploy",
    "audit": "npm audit --audit-level=high"
  },
  "devDependencies": {
    "twilio-run": "^5.0.1"
  },
  "engines": {
    "node": "20"
  },
  "overrides": {
    "title": "^4.0.1",
    "file-type": "^21.3.4"
  }
}
